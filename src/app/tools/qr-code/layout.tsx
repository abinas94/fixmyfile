import type { Metadata } from "next";
export const metadata: Metadata = { title: "QR Code Generator Free - Create QR for URL, UPI, WiFi | FixMyFile", description: "Generate QR codes for free. Create QR codes for URLs, UPI payments, WiFi, email, phone numbers. Download as PNG. Custom colors supported." };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
