import type { Metadata } from "next";
export const metadata: Metadata = { title: "Split PDF Online Free - Extract Pages from PDF | FixMyFile", description: "Split PDF into multiple files or extract specific pages. Free online tool, no upload required. Works instantly in your browser." };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
