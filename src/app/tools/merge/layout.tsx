import type { Metadata } from "next";
export const metadata: Metadata = { title: "Merge PDF Online Free - Combine PDFs Instantly | FixMyFile", description: "Merge multiple PDF files into one document for free. No upload, no sign-up. Works 100% in your browser. Drag, reorder, and combine PDFs instantly." };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
