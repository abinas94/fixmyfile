import type { Metadata } from "next";
export const metadata: Metadata = { title: "Scan to PDF Free - Document Scanner Online | FixMyFile", description: "Scan documents to PDF using your phone camera. Auto-enhance makes text sharp and background white like a real scanner. Free, no app install needed." };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
