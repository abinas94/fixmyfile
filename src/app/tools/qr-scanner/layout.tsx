import type { Metadata } from "next";
export const metadata: Metadata = { title: "QR Code Scanner Online Free - Scan QR, UPI, Barcode with Camera | FixMyFile", description: "Free QR code and barcode scanner using your phone camera. Scan UPI payment codes, product barcodes, URLs instantly. No app install needed." };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
