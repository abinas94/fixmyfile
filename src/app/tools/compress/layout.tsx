import type { Metadata } from "next";
export const metadata: Metadata = { title: "Compress PDF Online Free - Reduce PDF Size | FixMyFile", description: "Compress PDF files and reduce size without losing quality. Free online PDF compressor that works in your browser. No file upload needed." };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
