import type { Metadata } from "next";
export const metadata: Metadata = { title: "Passport Photo Maker India - Free Online Passport Size Photo | FixMyFile", description: "Create passport size photos for Indian passport, Aadhaar, PAN card, visa. Print multiple copies on A4 sheet. Free, no app needed, works in browser." };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
