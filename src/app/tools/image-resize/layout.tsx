import type { Metadata } from "next";
export const metadata: Metadata = { title: "Resize Image Online Free - Change Image Dimensions | FixMyFile", description: "Resize images to exact dimensions or percentage. Free online image resizer with aspect ratio lock. Supports JPG, PNG, WebP. No upload needed." };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
