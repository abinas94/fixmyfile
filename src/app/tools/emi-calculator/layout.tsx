import type { Metadata } from "next";
export const metadata: Metadata = { title: "EMI Calculator India - Home Loan, Car Loan, Personal Loan | FixMyFile", description: "Calculate monthly EMI for home loan, car loan, personal loan. Free EMI calculator with interest breakdown and visual chart. Indian banks rates supported." };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
