import type { Metadata } from "next";
export const metadata: Metadata = { title: "AI Image Upscaler Free - Enhance Image Quality & Resolution | FixMyFile", description: "Upscale and enhance low-resolution images using AI. Increase image resolution 2x without losing quality. Free, no sign-up, works in your browser." };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
