import type { Metadata } from "next";
export const metadata: Metadata = { title: "GST Calculator India - Calculate CGST SGST (5% 12% 18% 28%) | FixMyFile", description: "Free GST calculator for India. Calculate GST amount with CGST and SGST breakdown. Supports all GST rates: 5%, 12%, 18%, 28%. Add or remove GST instantly." };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
