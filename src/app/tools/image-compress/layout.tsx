import type { Metadata } from "next";
export const metadata: Metadata = { title: "Compress Image Online Free - Reduce Image Size | FixMyFile", description: "Compress images and reduce file size with adjustable quality. Supports JPG, PNG, WebP. Free online image compressor, no upload to server." };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
