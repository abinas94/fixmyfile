import type { Metadata } from "next";
export const metadata: Metadata = { title: "Word Document Formatter - Auto Format DOCX Online Free | FixMyFile", description: "Auto-format Word documents in one click. Fix fonts, spacing, margins, headings, numbering, tables, and images. Supports APA, IEEE, corporate, government formats." };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
