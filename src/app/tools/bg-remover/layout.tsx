import type { Metadata } from "next";
export const metadata: Metadata = { title: "Remove Background from Image Free Online - No Sign Up | FixMyFile", description: "Remove image background instantly for free. Download transparent PNG. No watermark, no sign-up, no upload to server. Works in your browser." };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
