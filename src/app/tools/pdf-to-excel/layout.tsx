import type { Metadata } from "next";
export const metadata: Metadata = { title: "PDF to Excel Converter Free - Extract Tables from PDF to CSV | FixMyFile", description: "Convert PDF tables to Excel/CSV format for free. Automatically detects table structure and exports as CSV. Opens in Excel and Google Sheets." };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
